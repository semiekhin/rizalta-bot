# RIZALTA Web App — Текущий статус

**Версия:** 0.3.1
**Дата обновления:** 2026-01-26
**URL:** https://webapp.rizaltaservice.ru/

## 📊 Прогресс: 85%

## ✅ Что работает

### Backend (app.py)
| Endpoint | Статус | Описание |
|----------|--------|----------|
| `GET /api/lots` | ✅ | Список лотов (прокси к PROD) |
| `GET /api/health` | ✅ | Healthcheck |
| `POST /api/calculate-roi` | ✅ | Расчёт ROI |
| `POST /api/book-showing` | ✅ | Заявка на показ |
| `POST /api/installment` | ✅ | Варианты рассрочки |
| `POST /api/generate-kp` | ✅ | Генерация PDF КП |
| `POST /api/generate-xlsx` | ✅ | Генерация Excel ROI |
| `GET /api/download-xlsx/{code}` | ⚠️ | GET для мобильных (ДОБАВЛЕН, НЕ ПРОТЕСТИРОВАН) |
| `POST /api/compare-deposit` | ✅ | Сравнение с депозитом |

### Frontend (LotDetail.jsx)
| Кнопка | Статус |
|--------|--------|
| Получить КП | ✅ |
| Варианты оплаты | ✅ |
| Расчёт доходности | ✅ |
| Сравнить с депозитом | ✅ |
| Скачать Excel | ⚠️ НА ПК работает, НА МОБИЛЬНЫХ — нужно тестировать GET endpoint |
| Записаться на показ | ✅ |

### Home.jsx
| Кнопка | Статус |
|--------|--------|
| Выбрать апартамент | ✅ |
| AI Консультант | ✅ |
| Расчёт доходности | ✅ (исправлено — теперь ведёт в каталог) |

## 🔧 Последние изменения (26.01.2026)

### v0.3.1
- Исправлена кнопка "Расчёт доходности" на главной (не было onClick)
- Добавлен GET endpoint `/api/download-xlsx/{code}` для мобильных
- Обновлён handleExcelDownload — использует window.open() вместо blob

### v0.3.0
- Добавлены endpoints: installment, generate-kp, generate-xlsx, compare-deposit
- Обновлён LotDetail.jsx — все кнопки и модалки
- Исправлены пути к БД
- Установлены зависимости: openpyxl, requests

## 🐛 Известные проблемы

1. **GET /api/download-xlsx/{code}** — добавлен в app.py (строка 122), но возвращает 405. Нужно проверить код endpoint и перезапустить backend.

## 📁 Ключевые файлы

- `/opt/webapp/backend/app.py` — основной API (версия 0.3.0)
- `/opt/webapp/frontend/src/pages/LotDetail.jsx` — карточка лота
- `/opt/webapp/frontend/src/pages/Home.jsx` — главная страница
