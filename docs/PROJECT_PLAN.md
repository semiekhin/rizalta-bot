# RIZALTA Web App — План проекта

## 🎯 Цель
Создать standalone веб-приложение как резервный канал на случай блокировки/падения Telegram.
Приложение должно дублировать основные функции бота для риэлторов.

## 📊 Почему это важно
- Telegram уже блокировали в РФ (2018-2020)
- Бизнес зависит от одного канала — это риск
- Веб-приложение = независимость + лучший брендинг

## 🏗️ Архитектура
```
СУЩЕСТВУЮЩЕЕ (не трогаем):
├── PROD /opt/bot         → @RealtMeAI_bot (webhook :8000)
├── DEV  /opt/bot-dev     → @rizaltatestdevop_bot (polling)
└── Mini App              → Vercel (шахматка в Telegram)

НОВОЕ (параллельно):
└── Web App /opt/webapp   → webapp.rizaltaservice.ru (БЛОКЕР!)
    ├── Frontend: React + Tailwind (mobile-first)
    ├── Backend: FastAPI :8003
    └── Данные: читает из PROD API (read-only)
```

## ✅ Что сделано

### Этап 1: Инфраструктура ✅
- [x] Создана структура /opt/webapp/
- [x] Backend FastAPI на порту 8003
- [x] Проксирование /api/lots к PROD
- [x] Python venv с зависимостями

### Этап 2: Frontend ✅
- [x] React + Vite + Tailwind
- [x] Mobile-first дизайн
- [x] 4 страницы:
  - Home (лендинг, статистика, быстрые действия)
  - Catalog (шахматка с фильтрами, 356 лотов)
  - LotDetail (детали апартамента, планировка)
  - Chat (AI консультант, пока заглушка)
- [x] Нижняя навигация
- [x] Production build

### Этап 3: Деплой ❌ БЛОКЕР
- [x] Vercel: https://rizalta-webapp.vercel.app/ — РАБОТАЕТ (но нужен VPN из РФ)
- [x] Cloudflare Pages: rizalta-webapp.semiekhin.workers.dev — работает
- [ ] ❌ webapp.rizaltaservice.ru — ERR_HTTP2_PING_FAILED

## 🔴 ТЕКУЩАЯ ПРОБЛЕМА

### Суть
Любой путь через Cloudflare (туннель, прокси, Pages) даёт ошибку ERR_HTTP2_PING_FAILED при загрузке JS файла (209KB) в браузере.

### Что пробовали (всё не работает):
1. **Cloudflare Tunnel** → статика застревает (pending)
2. **Cloudflare Proxy + Vercel** → ERR_HTTP2_PING_FAILED
3. **Cloudflare Pages** → ERR_HTTP2_PING_FAILED
4. **Vercel напрямую (DNS only)** → IP Vercel недоступен из РФ сети

### Что работает:
- curl с сервера — всё отдаётся за 0.1 сек
- rizalta-webapp.vercel.app — работает (но только с VPN)
- rizalta-webapp.semiekhin.workers.dev — работает (но не .ru домен)

### Вывод:
Проблема между браузером и Cloudflare на уровне HTTP/2. Не зависит от браузера (Chrome, Safari — одинаково).

## 🔜 Варианты решения (не проверены)

1. **Отключить HTTP/2 в Cloudflare** — если есть такая настройка
2. **Code splitting** — разбить JS на куски < 50KB
3. **Отдельный домен** — купить rizalta.ru, направить на Vercel без Cloudflare
4. **Российский хостинг** — для сценария "суверенный интернет"

## 📈 Прогресс
```
[██████████░░░░░░░░░░] 50%

✅ Инфраструктура    100%
✅ Frontend UI       100%
❌ Деплой             0%  ← БЛОКЕР
⬜ Чат GPT            0%
⬜ Кнопки действий    0%
⬜ Авторизация        0%
```

## 📁 Ключевые файлы
```
/opt/webapp/
├── frontend/
│   ├── src/pages/       # Home, Catalog, LotDetail, Chat
│   ├── dist/            # Production build (готов!)
│   └── vite.config.js
├── backend/
│   └── app.py           # API + статика
└── docs/
    ├── PROJECT_PLAN.md  # Этот файл
    └── WEBAPP_CONTEXT.md
```

## 📚 Хостинг (что где)
- **Vercel**: rizalta-webapp.vercel.app (работает с VPN)
- **Cloudflare Pages**: rizalta-webapp.semiekhin.workers.dev (работает)
- **Домен**: webapp.rizaltaservice.ru → Cloudflare Pages (НЕ РАБОТАЕТ)
