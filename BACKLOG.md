# BACKLOG — Невыполненные задачи

## P0 — Срочно

- [x] **Деплой v0.9.8 в PROD** — задеплоено 01.04.2026
- [x] **RCLICK fix** — задеплоено 21.04.2026 (PHPSESSID auth + auto-relogin + multipart)
- [~] **Стабилизация YandexGPT** — agentic loop уже на gpt-oss-120b, проверить стабильность, убрать OpenAI fallback. **Частично закрыта 21.05.2026:** фундаментальные бизнес-правила вынесены в `config/project_knowledge.txt` (верхний слой system prompt), что устранило галлюцинации по критичным фактам (инцидент «можно жить»). Полное закрытие — после 2–3 недель мониторинга PROD.
- [ ] **RAG расширение** — добавить аналитику CoreXP, расширить chunking, тестировать качество ответов
- [ ] **Удалить бэкапы (после 28.04.2026)** — `/opt/webapp-dev/venv.backup-20260421_1012`, `/opt/webapp/backend/webapp.db.bak-20260421_1020`

## P1 — Следующая итерация

- [x] **Календарь показов — этап 1+2** — задеплоено в PROD 05.05.2026 (v0.9.8). Календарь /shows + дашборд /shows/dashboard-84a11b0664 (метрики по 4 менеджерам, % броней, красные флаги ≥7 дней).
- [x] **Дашборд показов — toggle + drill-down + «Показать» + сортировка** — задеплоено в PROD 14.05.2026 (v0.9.11 + v0.9.12). См. SESSION_LOG 14.05.2026.
- [x] **Дашборд: Итого над списком (mobile) + кликабельное «Из них с бронью»** — задеплоено в PROD 17.05.2026 (v0.9.13). См. SESSION_LOG 17.05.2026.
- [ ] **Restoration drill бэкапа `shows.db`** — на свежем 20:01-бэкапе (запланировано 15.05.2026 утром): развернуть `shows-YYYY-MM-DD.db.gz` во временный файл, проверить целостность и count, прогнать процедуру восстановления из CLAUDE.md.
- [ ] **PDF из чата** — кнопка "Скачать PDF" после генерации отчёта в чате
- [ ] **System prompt YandexGPT** — расширить статистикой лотов real-time, улучшить качество ответов
- [ ] **Портфельный калькулятор** — отдельный экран (код ready, деприоритизирован — менеджеры составляют портфели вручную)

## P2 — Улучшения

- [x] **Справочник агентств (фаза 1)** — задеплоено 06.05.2026 (v0.9.9). 26 агентств, `<datalist>` подсказки в обеих модалках, свободный ввод сохранён.
- [x] **Статистика по агентствам в дашборде (фаза 2)** — задеплоено 14.05.2026 (v0.9.11). Toggle «По агентствам» в `/stats?group_by=agency`, метрики/% броней/сортировка по агентствам.
- [x] **Матрица «менеджер × агентство»** — закрыто через drill-down (v0.9.11): тап на карточку менеджера → агентства этого менеджера, тап на агентство → его менеджеры. Интерактивный разрез вместо статической heatmap; отдельный `/matrix`-эндпоинт не понадобился.
- [ ] **Очистить tracked venv-файлы** — `git rm -r --cached venv/` (1930 файлов закоммичены до добавления `venv/` в `.gitignore`, из-за чего `git add -A` всегда тянет venv-шум в коммиты). Отдельный коммит «chore: untrack venv». Локальная проверка после: `git status` без venv-строк, `git ls-files venv | wc -l` = 0.
- [ ] **Справочник агентств — мердж дублей и пополнение списка.** Разведка 17.05.2026 (Группа A): в PROD `shows.db` три написания живут параллельно — `ПроСторе Сочи` (13), `Простор` (8), `Простор Сочи` (2) — суммарно 23 показа, мердж в каноническое `ПроСторе Сочи`. Добавить новое `КП Реал Эстейт` в `AGENCIES` (в БД пока 0 показов). Переименовать `Рич` (1 показ) → `Рич групп`. Правки: `AGENCIES` в `shows_service.py` (3 строки) + UPDATE на `realtor_agency` в `shows.db` (PROD+DEV) с обязательным бэкапом до. Не входит в эту задачу: 9 free-text названий помимо `AGENCIES` (`Лехто ИП`, `Домрил` vs `ДомРил` дубль, `АН Самолёт г. Самара`, `Блэкаут`, `ИП Лехто`, `ИП Панченко`, `ИП Самсонова Л.Ю. Краснодар`, `Лето` vs `Лето Недвижимость`, `Сириус` vs `Сириус Групп`) и 4 показа с пустым `realtor_agency` — требует решения с Сергеем (норм/дубль/удалить из дропдауна).
- [ ] **Унификация роли AI в `instructions.txt` стр. 4–6.** Вступление обращается к AI как к ассистенту клиента («помогаешь человеку понять проект»), тогда как `ADVISOR_INSTRUCTION` и новый `project_knowledge.txt` задают роль ассистента для риэлторов / отдела продаж. Конфликт в simple-chat пути (стр. 779 `ai_chat.py`), где `ADVISOR_INSTRUCTION` НЕ подключается. На инцидент 09.05.2026 это не повлияло (закрыто через project_knowledge), но при будущем ревью промпт-стека unification роли нужна. Затрагивает `backend/config/instructions.txt` стр. 1–43 — потребует переосмысления, не точечной правки.
- [ ] **К4 whitelist** — инфраструктура готова (webapp.db, access_tokens, auth.js закомментированы с TODO)
- [ ] **Yandex Vector Store API** — замена TF-IDF на векторный поиск для RAG (лучше качество)
- [ ] **Сдвиг финмодели для К3/К4** — display срока сдачи учитывает корпус (К3/К4 → 2 кв. 2028), но ROI/окупаемость считается по модели "сдача 2027, аренда с 2028" для всех корпусов. Нужен сдвиг расчётной модели на год: `calculations.py`, `calc_universal.py`, `calc_xlsx_generator.py`, `investment_calc.py`, `calculator.py`, `investment_compare.py`. Иначе для К3/К4 в одном PDF "сдача 2 кв. 2028" соседствует с расчётом аренды, начинающейся в 2028.
- [ ] **Унификация бизнес-правила малых лотов в трёх проектах.** Сейчас `CUSTOM_INSTALLMENT_UNITS` существует тремя независимыми реализациями: `/opt/bot` и `/opt/webapp` (хардкод-список + проверка `building==1`) и `/opt/bot-max` (проверка по площади `≤22.1 м²` через `config/settings.py:CUSTOM_INSTALLMENT_MAX_AREA` — это правильная архитектура, нет ручного ведения списка). Триггер: 06.05.2026 в webapp обнаружен устаревший список из 8 кодов (отстал на 5 лотов от бота, плюс лишний А101, плюс отсутствовала проверка building). Минимум 3 КП с неправильной 18-мес. рассрочкой ушли клиентам 4 мая. Цель: общий источник правды. Кандидаты: (a) JSON-конфиг `data/installment_config.json` в общей директории, читается тремя проектами; (b) колонка в `properties.db.units` (например, `has_custom_installment BOOL`) или отдельная таблица `installment_rules`; (c) перейти везде на правило по площади (как в bot-max, `≤22.1 м²`) — самый радикальный и самый правильный. Решить при взятии в работу. Затрагивает три проекта, требует координации деплоев.
- [ ] **Скрыть кнопку «Полное КП» в `LotDetail.jsx` для custom-лотов.** Сейчас кнопка показывается всегда; для В225/В217/В717 (К1, 22.1 м²) клик отдаёт файл с правильным содержимым (12-мес. без 18-мес., см. backend force в `kp_pdf_generator.py`), но имя кнопки вводит в заблуждение. Триггер: хотфикс 06.05.2026 закрыл юридический риск через backend force `include_18m=False` для custom-лотов. Frontend-доработка оставлена на потом. Реализация: добавить в `/api/lot/{code}` поле `is_custom` (возвращается из бэкенда по тому же правилу что в `kp_pdf_generator.py`), на фронте условный рендер кнопки. Лучше делать в составе задачи унификации (см. выше) — тогда `is_custom` станет первым клиентом нового источника правды.

## P3 — Техдолг

- [ ] **Тесты** — нет тестов, добавить хотя бы smoke-тесты для критических эндпоинтов
- [ ] **Линтеры** — настроить flake8/black для backend
- [ ] **Очистка legacy** — OpenAI env переменные (OPENAI_API_KEY, OPENAI_MODEL) больше не используются
- [ ] **corp3_units.json** — больше не используется (всё в properties.db), CORP3_DATA_PATH в .env можно убрать
- [ ] **ShowsDashboard.jsx: при 6+ менеджерах рассмотреть `lg:grid-cols-3` на десктопе** — сейчас `grid-cols-2` / `sm:grid-cols-2`, при 6 карточках ряды тянутся вниз
- [ ] **Регулярная ревизия `.env`** — раз в квартал проходить по всем ключам в `backend/.env` (DEV+PROD), сверять что каждый адрес/токен/ID реально используется. Поводом стало обнаружение `dreaming2015@mail.ru` в `MANAGER_EMAIL` неизвестного происхождения (06.05.2026). Чек-лист: для каждого email-адреса понять кто получатель; для каждого `*_TOKEN`/`*_ID` понять какой сервис.
- [ ] **Bump версии перед `git push`** — процессное правило. `deploy-to-prod.sh` ругается «Versions match — Continue anyway? (y/N)» если `app.py` версия не менялась, что бьёт UX и заставляет вручную подтверждать. Решения: (a) дисциплина в коммите (просто помнить), (b) pre-push hook, который сравнивает `version=` в `app.py` с тегом последнего пуша и блокирует. Вариант (a) пока достаточен.
- [ ] **CLAUDE.md: устарел cron бэкапа `shows.db`** — в разделе «Бэкап shows.db» указано `30 3 * * *`, реально с 14.05.2026 cron на PROD `1 20 * * *` (перенос на 20:01 МСК + Telegram-уведомление в `ADMIN_CHAT_ID`). Обновить раздел при ближайшей правке CLAUDE.md.
